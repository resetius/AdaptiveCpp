name: macOS Metal backend

on:
  # TEMPORARY: not limited to develop, so that this branch is tested too
  push:
  pull_request:

jobs:
  build_metal:
    name: Metal backend [macOS, LLVM 20]
    # TEMPORARY: the xcode-27 image, in public preview, runs macOS 27.0 (26A428)
    runs-on: xcode-27
    steps:
    - uses: actions/checkout@v7
      with:
        submodules: 'recursive'

    - name: install dependencies
      run: |
        set +e
        brew update
        brew install llvm@20 lld@20 libomp boost ninja lit
        set -e

    - name: fetch metal-cpp
      run: |
        git clone --depth 1 --branch release/metal-cpp_macOS27_iOS27 \
          https://github.com/apple/metal-cpp.git ${GITHUB_WORKSPACE}/metal-cpp

    - name: build AdaptiveCpp
      run: |
        LLVM_PREFIX=$(brew --prefix llvm@20)
        LLD_PREFIX=$(brew --prefix lld@20)
        OMP_PREFIX=$(brew --prefix libomp)
        METAL_CPP_DIR=${GITHUB_WORKSPACE}/metal-cpp

        mkdir build && cd build
        cmake .. -GNinja \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_C_COMPILER=/usr/bin/clang \
          -DCMAKE_CXX_COMPILER=/usr/bin/clang++ \
          -DCLANG_EXECUTABLE_PATH=${LLVM_PREFIX}/bin/clang++ \
          -DLLVM_DIR=${LLVM_PREFIX}/lib/cmake/llvm \
          -DCMAKE_OSX_SYSROOT="$(xcrun --show-sdk-path)" \
          -DCMAKE_INSTALL_PREFIX=${GITHUB_WORKSPACE}/install \
          -DWITH_OPENCL_BACKEND=OFF \
          -DWITH_METAL_BACKEND=ON \
          -DACPP_COMPILER_FEATURE_PROFILE=full \
          -DACPP_LLD_PATH=${LLD_PREFIX}/bin/ld64.lld \
          -DMETAL_INCLUDE_DIR=${METAL_CPP_DIR} \
          -DSEQUENTIAL_LINK_LINE=${OMP_PREFIX}/lib/libomp.dylib \
          "-DOMP_LINK_LINE=-fopenmp ${OMP_PREFIX}/lib/libomp.dylib -L${OMP_PREFIX}/lib/" \
          "-DOMP_CXX_FLAGS=-fopenmp -D_ENABLE_EXTENDED_ALIGNED_STORAGE -I${OMP_PREFIX}/include"
        ninja install

    - name: build generic SSCP tests
      run: |
        mkdir ${GITHUB_WORKSPACE}/build/tests-sscp
        cd ${GITHUB_WORKSPACE}/build/tests-sscp
        cmake -GNinja \
          -DACPP_TARGETS="generic" \
          -DAdaptiveCpp_DIR=${GITHUB_WORKSPACE}/install/lib/cmake/AdaptiveCpp \
          -DWITH_PCUDA_TESTS=ON \
          -DACPP_TEST_WORK_GROUP_SHUFFLE_EXT=ON \
          ${GITHUB_WORKSPACE}/tests
        ninja

    - name: acpp-info
      run: |
        DYLD_LIBRARY_PATH=${GITHUB_WORKSPACE}/install/lib \
          ${GITHUB_WORKSPACE}/install/bin/acpp-info | grep -iE "Name:|gpufamilies|sub_group|compute_units" | head -12

    # TEMPORARY: only the 64-bit fetch_add case, with a time limit so a hang is
    # reported instead of running until the job times out
    - name: probe 64-bit fetch_add
      run: |
        cd ${GITHUB_WORKSPACE}/ci-probe
        ${GITHUB_WORKSPACE}/install/bin/acpp --acpp-targets=generic -O2 \
          -o fetch_add_probe fetch_add_probe.cpp
        ${GITHUB_WORKSPACE}/install/bin/acpp --acpp-targets=generic -O2 \
          -o fetch_add_scatter fetch_add_scatter.cpp
        for n in 32 256 2048; do
          echo "=== one address, n=$n"
          DYLD_LIBRARY_PATH=${GITHUB_WORKSPACE}/install/lib \
            perl -e 'alarm 180; exec @ARGV' ./fetch_add_probe $n || echo "FAILED or timed out (exit $?)"
        done
        for n in 32 2048; do
          echo "=== one address per item, n=$n"
          DYLD_LIBRARY_PATH=${GITHUB_WORKSPACE}/install/lib \
            perl -e 'alarm 180; exec @ARGV' ./fetch_add_scatter $n 4 || echo "FAILED or timed out (exit $?)"
        done

    # TEMPORARY: one operation per process, with the generated shader dumped, so
    # that the operation a device refuses is named and its source can be taken
    - name: probe every 64-bit operation
      run: |
        cd ${GITHUB_WORKSPACE}/ci-probe
        ${GITHUB_WORKSPACE}/install/bin/acpp --acpp-targets=generic -O2 \
          -o atomic64_ops atomic64_ops.cpp
        mkdir -p ${GITHUB_WORKSPACE}/msl-dump
        for op in add add_used sub add_i64 sub_i64 exchange load store and or xor min max cas; do
          echo "=== $op"
          mkdir -p ${GITHUB_WORKSPACE}/msl-dump/$op
          ACPP_METAL_DUMP_DIRECTORY=${GITHUB_WORKSPACE}/msl-dump/$op \
          DYLD_LIBRARY_PATH=${GITHUB_WORKSPACE}/install/lib \
            perl -e 'alarm 120; exec @ARGV' ./atomic64_ops $op 2048 \
            || echo "$op FAILED (exit $?)"
        done

    - name: run Metal tests (sycl)
      run: |
        cd ${GITHUB_WORKSPACE}/build/tests-sscp
        DYLD_LIBRARY_PATH=${GITHUB_WORKSPACE}/install/lib ./sycl_tests

    - name: run Metal tests (pcuda)
      run: |
        cd ${GITHUB_WORKSPACE}/build/tests-sscp
        DYLD_LIBRARY_PATH=${GITHUB_WORKSPACE}/install/lib ./pcuda_tests

    - name: run Metal tests (lit sscp)
      run: |
        cd ${GITHUB_WORKSPACE}/build/tests-sscp
        DYLD_LIBRARY_PATH=${GITHUB_WORKSPACE}/install/lib \
        PATH=$(brew --prefix llvm@20)/bin:$PATH \
        ninja check-sscp

    - name: run Metal tests (lit reflection)
      run: |
        cd ${GITHUB_WORKSPACE}/build/tests-sscp
        DYLD_LIBRARY_PATH=${GITHUB_WORKSPACE}/install/lib \
        PATH=$(brew --prefix llvm@20)/bin:$PATH \
        ninja check-reflection

    - name: upload the dumped shaders
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: msl-dump
        path: ${{ github.workspace }}/msl-dump
